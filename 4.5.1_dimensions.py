# tuples
dimensions = (200, 50)  # 如果元组中只包含一个元素，创建时必须加上逗号。
print(dimensions[0])
print(dimensions[1])

for dimension in dimensions:
    print(dimension)

dimensions = (400, 100)
print('\nModified dimensions:')
for dimension in dimensions:
    print(dimension)
